[extend]
useDefault = true

[allowlist]
  description = "Allowlist for intentionally public values"
  # IndexNow key is public by design — served at /<key>.txt for domain verification.
  # Fingerprint from historical commit before key was moved to a secret.
  commits = ["cd6c46aee2ac592a30c93b8ceb5fa2aad32987d7"]
