[extend]
useDefault = true

[allowlist]
  description = "Allowlist for intentionally public values"
  # IndexNow key is public by design — served at /<key>.txt for domain verification
  # and referenced in .github/workflows/indexnow.yml. Allowlisted to prevent false positives.
  commits = ["2766905b2d5b288e6245d37bc794a78bcc5ddd48"]
